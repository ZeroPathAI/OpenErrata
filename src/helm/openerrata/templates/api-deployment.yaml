apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "openerrata.fullname" . }}-api
  labels:
    {{- include "openerrata.labels" . | nindent 4 }}
    app.kubernetes.io/component: api
spec:
  replicas: {{ .Values.replicaCount.api }}
  selector:
    matchLabels:
      {{- include "openerrata.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: api
  template:
    metadata:
      labels:
        {{- include "openerrata.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: api
    spec:
      serviceAccountName: {{ include "openerrata.serviceAccountName" (dict "root" . "component" "api") }}
      automountServiceAccountToken: false
      containers:
        - name: api
          image: {{ include "openerrata.image" . | quote }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command: ["node", "build/index.js"]
          ports:
            - containerPort: 3000
              protocol: TCP
          envFrom:
            - secretRef:
                name: {{ include "openerrata.secretName" . }}
            - configMapRef:
                name: {{ include "openerrata.fullname" . }}
          livenessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 10
